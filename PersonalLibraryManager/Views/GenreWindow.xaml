<Window x:Class="PersonalLibraryManager.Views.GenreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonalLibraryManager.Views"
        mc:Ignorable="d"
        Title="Genres" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding Genres}" SelectedItem="{Binding SelectedGenre}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Add" Command="{Binding AddCommand}" Width="80" Margin="5"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="5"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>

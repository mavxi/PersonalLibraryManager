<Window x:Class="PersonalLibraryManager.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonalLibraryManager"
        mc:Ignorable="d"
        Title="About Personal Library Manager" Height="200" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Personal Library Manager" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="Version 1.0" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        <TextBlock Grid.Row="2" Text="A simple application to manage your personal book library" HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
        <Button Grid.Row="4" Content="OK" Width="80" HorizontalAlignment="Center" Click="OKButton_Click"/>
    </Grid>
</Window>
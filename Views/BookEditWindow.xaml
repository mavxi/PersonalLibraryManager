<Window x:Class="PersonalLibraryManager.Views.BookEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonalLibraryManager.ViewModels"
        mc:Ignorable="d"
        Title="Book Details" Height="450" Width="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Title:"/>
        <TextBox Grid.Row="1" Text="{Binding Book.Title}"/>

        <Label Grid.Row="2" Content="Author:"/>
        <TextBox Grid.Row="3" Text="{Binding Book.Author}"/>

        <Label Grid.Row="4" Content="ISBN:"/>
        <TextBox Grid.Row="5" Text="{Binding Book.ISBN}"/>

        <Label Grid.Row="6" Content="Genre:"/>
        <TextBox Grid.Row="7" Text="{Binding Book.Genre}"/>

        <Label Grid.Row="8" Content="Publication Year:"/>
        <TextBox Grid.Row="9" Text="{Binding Book.PublicationYear}"/>

        <Label Grid.Row="10" Content="Description:"/>
        <TextBox Grid.Row="11" Text="{Binding Book.Description}" TextWrapping="Wrap" AcceptsReturn="True" Height="80"/>

        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <CheckBox Content="Read" IsChecked="{Binding Book.IsRead}" Margin="0,0,20,0" VerticalAlignment="Center"/>
            <Label Content="Rating:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Book.Rating}" Width="50" Margin="0,0,20,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>